bundle.js is a compiled script that uses terminal command npm run deploy.

It compresses the javascript files: 
framework.js, 
main.js, 
noise.js,

and one of each shader (fragment and vertex):

the interaction is a 3d space that symbolizes my mind as an architect
